---
复习: "1"
---



# 题目
给你一个链表，两两交换其中相邻的节点，并返回交换后链表的头节点。你必须在不修改节点内部的值的情况下完成本题（即，只能进行节点交换）。

**示例 1：**

![](https://assets.leetcode.com/uploads/2020/10/03/swap_ex1.jpg)

**输入：** head = \[1,2,3,4]
**输出：**\[2,1,4,3]

**示例 2：**

**输入：** head = \[]
**输出：** \[]

**示例 3：**

**输入：** head = \[1]
**输出：** \[1]


# 题解

```java
class Solution {
    public ListNode swapPairs(ListNode head) {
        if(head==null || head.next==null) return head;
        ListNode ret = head.next;
        head.next = swapPairs(head.next.next);
        ret.next = head;
        return ret;
    }
}
```

递归思路，注意操控节点指针的时候，不要形成环，否则会栈溢出




一刷：
迭代法
```java
class Solution {
    public ListNode swapPairs(ListNode head) {
        // 1. 创建哑节点，方便处理头部的交换
        ListNode dummy = new ListNode(0);
        dummy.next = head;
        ListNode temp = dummy;

        // 2. 只有当后面至少还有两个节点时，才进行交换
        while (temp.next != null && temp.next.next != null) {
            ListNode node1 = temp.next;
            ListNode node2 = temp.next.next;

            // 3. 开始“大挪移” (交换 node1 和 node2)
            temp.next = node2;       // 第一步：前驱指向 node2
            node1.next = node2.next; // 第二步：node1 指向后面剩下的部分
            node2.next = node1;      // 第三步：node2 指向 node1

            // 4. 移动 temp 指针，准备处理下一对
            temp = node1; 
        }

        return dummy.next;
    }
}
```
