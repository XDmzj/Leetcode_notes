---
复习: "1"
---

# 题目

给定两个字符串 `s` 和 `p`，找到 `s` 中所有 `p` 的 **异位词** 的子串，返回这些子串的起始索引。不考虑答案输出的顺序。

**示例 1:**

**输入:** s = "cbaebabacd", p = "abc"
**输出:** \[0,6]
**解释:**
起始索引等于 0 的子串是 "cba", 它是 "abc" 的异位词。
起始索引等于 6 的子串是 "bac", 它是 "abc" 的异位词。

 **示例 2:**

**输入:** s = "abab", p = "ab"
**输出:** \[0,1,2]
**解释:**
起始索引等于 0 的子串是 "ab", 它是 "ab" 的异位词。
起始索引等于 1 的子串是 "ba", 它是 "ab" 的异位词。
起始索引等于 2 的子串是 "ab", 它是 "ab" 的异位词。



# 题解


```java
class Solution {
    public List<Integer> findAnagrams(String s, String p) {
        int[] cntp = new int[26]; 
        for(char c : p.toCharArray()){
            cntp[c-'a']++;  存储字符串p中26字母出现的次数
        }
        List<Integer> ans = new ArrayList<>();
        int[] cnts = new int[26];
        for(int right = 0 ; right < s.length() ; right++){  
            cnts[s.charAt(right) - 'a']++;  存储子串中26字母出现的次数
            int left = right-p.length() +1; 
            if(left<0) continue; 还没提取出一个完整的等长的子串
            if(Arrays.equals(cnts,cntp)){
                ans.add(left);符合条件则将下标left存入ans
            }
            cnts[s.charAt(left)-'a']--;无论符不符合条件都向前走，将多出来的元素去掉
        }        
        return ans;
    }
}
```

我没想到的是：只管right向前走，left由right和p的长度确定

方法补充：p.toCharArray()
Arrays.equals(l1,l2)
s.charAt(index)

`s.charAt()` 是一个非常基础且常用的方法，用于**获取字符串中指定位置的字符**。
```java
String s = "Gemini";
char result = s.charAt(0); 
System.out.println(result); // 输出: 'G'
```



一刷：toCharArray已经记住了
但是Arrays.equals、charAt还是没记住
不过这次我做出了自己的一些改变
