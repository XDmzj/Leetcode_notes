# 题目


给定两个字符串 `s` 和 `t`，长度分别是 `m` 和 `n`，返回 s 中的 **最短窗口 子串**，使得该子串包含 `t` 中的每一个字符（**包括重复字符**）。如果没有这样的子串，返回空字符串 `""`。

测试用例保证答案唯一。

**示例 1：**

**输入：** s = "ADOBECODEBANC", t = "ABC"
**输出：**"BANC"
**解释：** 最小覆盖子串 "BANC" 包含来自字符串 t 的 'A'、'B' 和 'C'。

**示例 2：**

**输入：** s = "a", t = "a"
**输出：**"a"
**解释：** 整个字符串 s 是最小覆盖子串。

**示例 3:**

**输入:** s = "a", t = "aa"
**输出:** ""
**解释:** t 中两个字符 'a' 均应包含在 s 的子串中，
因此没有符合条件的子字符串，返回空字符串。




# 题解

整体逻辑很长，值得慢慢看，理清每一步
- 利用ASCII码将字符出现次数存储在int数组中
- 滑动窗口的左窗口，在右窗口移动时继承，不要每次从零开始
- valid表示窗口内**满足数量要求**的字符**种类数**，（本质上是窗口内的字符种类数，加了数量要求）
- required表示need中字符种类数
- start、minLen，记录符合条件的子串



可以看着例子过一遍整体的逻辑，多过几次，感受need存储的方便，valid和required的精巧，start和minLen更新的思路，左窗口left收缩的条件，valid的+-


```java
public String minWindow(String s, String t) {
    if (s == null || t == null || s.length() < t.length()) return "";

    // 用数组代替 HashMap 记录字符出现次数，ASCII 码范围 128 足够
    int[] need = new int[128];
    int[] window = new int[128];
    
    // 统计目标字符串 t 中每个字符的需求量
    for (char c : t.toCharArray()) {
        need[c]++;
    }

    int left = 0, right = 0;
    int valid = 0; // 窗口中满足 need 条件的字符种类数
    int required = 0; // t 中不同字符的总数
    for (int i : need) {
        if (i > 0) required++;
    }

    // 记录最小覆盖子串的起始位置及长度
    int start = 0, minLen = Integer.MAX_VALUE;

    while (right < s.length()) {
        char c = s.charAt(right);
        right++; // 扩大窗口

        // 进行窗口内数据的一系列更新
        if (need[c] > 0) {
            window[c]++;
            if (window[c] == need[c]) {
                valid++;
            }
        }

        // 判断左侧窗口是否收缩（当窗口满足所有字符需求时）
        while (valid == required) {
            // 在这里更新最小覆盖子串
            if (right - left < minLen) {
                start = left;
                minLen = right - left;
            }

            char d = s.charAt(left);
            left++; // 缩小窗口

            // 进行窗口内数据的一系列更新
            if (need[d] > 0) {
                if (window[d] == need[d]) {
                    valid--;
                }
                window[d]--;
            }
        }
    }

    return minLen == Integer.MAX_VALUE ? "" : s.substring(start, start + minLen);
}
```


第二天又做一遍：
window添加元素时，只能添加`need[c]>0`的
valid，required的设置，记不清
left缩减时的valid、window更新逻辑没写对
更新start、minLen时逻辑不对，用了Math.min()，但这里不符合逻辑，
